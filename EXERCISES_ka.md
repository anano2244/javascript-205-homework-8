> 🇬🇧 [English version](./EXERCISES.md) · [← მიმოხილვაზე დაბრუნება](./README_ka.md) · [📤 როგორ ჩააბაროთ](./SUBMITTING_ka.md)

# საშინაო დავალება 8 — სავარჯიშოები

შექმენით ქვემოთ ჩამოთვლილი ფაილები `submissions/<your-username>/` ფოლდერში და დაწერეთ JavaScript კოდი თითოეული დავალებისთვის.

გამოიყენეთ მხოლოდ ის, რასაც ქვემოთ მოცემული [წესები](#წესები) უშვებს. თითოეული ფაილი შეგიძლიათ ასე გაუშვათ:

```bash
node exercise_1.js
```

ყოველ სავარჯიშოს **მოსალოდნელი შედეგი** უწერია. თქვენმა პროგრამამ ზუსტად ის უნდა დაბეჭდოს.

როცა მნიშვნელობას წარწერასთან ერთად ბეჭდავთ, წარწერა და მნიშვნელობა **ორ არგუმენტად** გადაეცით: `console.log("doubled:", doubled);` გამოიყენეთ ზუსტად ის წარწერები, რომლებიც მოსალოდნელ შედეგშია ნაჩვენები.

---

## `exercise_1.js` — `map`

ჩააკოპირეთ ეს სამი მასივი თქვენს ფაილში:

```javascript
const numbers = [1, 2, 3, 4, 5];
const names = ["nino", "gio", "mariam"];
const prices = [10, 25, 40];
```

მხოლოდ **`map`**-ის გამოყენებით — ციკლების გარეშე — შექმენით და დაბეჭდეთ:

1. `doubled` — თითოეული რიცხვი გამრავლებული `2`-ზე
2. `upper` — თითოეული სახელი დიდი ასოებით
3. `withTax` — თითოეული ფასი 18% დღგ-ს დამატებით (ფასი პლუს საკუთარი თავის 18%)
4. `lengths` — თითოეულ სახელში ასოების რაოდენობა

მოსალოდნელი შედეგი:

```text
doubled: [ 2, 4, 6, 8, 10 ]
upper: [ 'NINO', 'GIO', 'MARIAM' ]
withTax: [ 11.8, 29.5, 47.2 ]
lengths: [ 4, 3, 6 ]
```

> დააკვირდით: ოთხივე შედეგს ზუსტად იმდენი ელემენტი აქვს, რამდენიც იმ მასივს, საიდანაც წარმოიშვა. სწორედ ამას აკეთებს `map` ყოველთვის: ის არასდროს ამატებს ან აკლებს ელემენტებს, მხოლოდ თითოეულს ცვლის.

---

## `exercise_2.js` — `filter` და `find`

ჩააკოპირეთ ეს მასივი თქვენს ფაილში:

```javascript
const students = [
    { name: "Nino",   grade: 92, active: true  },
    { name: "Gio",    grade: 58, active: true  },
    { name: "Mariam", grade: 74, active: false },
    { name: "Luka",   grade: 45, active: true  },
    { name: "Ana",    grade: 88, active: false },
];
```

დაბეჭდეთ თითოეული:

1. `passedNames` — იმ სტუდენტების **სახელები**, ვისაც `60` ან მეტი ქულა აქვს
2. `activeCount` — რამდენი სტუდენტია `active`
3. `luka` — მთლიანი ობიექტი იმ სტუდენტისა, რომელსაც `"Luka"` ჰქვია
4. `firstFailing` — პირველი ისეთი სტუდენტის **სახელი**, რომლის ქულაც `60`-ზე ნაკლებია
5. `missing` — შედეგი იმისა, რომ ეძებთ სტუდენტს სახელად `"Dato"`, რომელიც მასივში არ არის

მოსალოდნელი შედეგი:

```text
passedNames: [ 'Nino', 'Mariam', 'Ana' ]
activeCount: 3
luka: { name: 'Luka', grade: 45, active: true }
firstFailing: Gio
missing: undefined
```

შემდეგ უპასუხეთ ამ კითხვას კომენტარში, ფაილის ბოლოში:

**რას აბრუნებს `filter` და რას აბრუნებს `find`? რა ხდება თითოეულ მათგანთან, როცა არაფერი ემთხვევა?**

> მინიშნება: პირველისთვის ორი მეთოდი დაგჭირდებათ, ერთმანეთის მიყოლებით. მეორესთვის თავად არაფრის დათვლა არ გჭირდებათ — მასივმა თავისი `.length` უკვე იცის. მეოთხესთვის `find` ობიექტს გაძლევთ, ამიტომ `.name` პირდაპირ გამოძახების შემდეგ იწერება.

---

## `exercise_3.js` — `reduce`

ჩააკოპირეთ ეს თქვენს ფაილში:

```javascript
const numbers = [4, 8, 15, 16, 23, 42];

const cart = [
    { item: "Bread",  price: 2,  quantity: 2 },
    { item: "Cheese", price: 12, quantity: 1 },
    { item: "Tea",    price: 5,  quantity: 3 },
];
```

ოთხივესთვის **`reduce`**-ის გამოყენებით დაბეჭდეთ:

1. `sum` — ყველა რიცხვის ჯამი
2. `total` — რა ღირს მთელი კალათა (ყოველი ხაზი არის `price × quantity`)
3. `itemCount` — სულ რამდენი ნივთია კალათაში
4. `biggest` — ყველაზე დიდი რიცხვი `numbers`-ში

მოსალოდნელი შედეგი:

```text
sum: 108
total: 31
itemCount: 6
biggest: 42
```

> მინიშნება მეოთხესთვის: აქამდე მიღებული შედეგი არის „ყველაზე დიდი რიცხვი, რაც აქამდე ვნახე“. დაიწყეთ `numbers[0]`-დან და ყოველ ნაბიჯზე დააბრუნეთ ორიდან უფრო დიდი. თუ `if`-ს `Math.max()`-ს ამჯობინებთ, callback-ის შიგნით მისი გამოყენება ნებადართულია.

---

## `exercise_4.js` — `some`, `every` და `forEach`

ჩააკოპირეთ ეს მასივი თქვენს ფაილში:

```javascript
const temperatures = [12, 18, 25, 30, 7];
```

დაბეჭდეთ ამ ოთხი კითხვის პასუხი ლოგიკური მნიშვნელობის (boolean) სახით:

1. `anyHot` — არის თუ არა რომელიმე ტემპერატურა `28`-ზე მეტი?
2. `allAboveZero` — ყველა ტემპერატურა `0`-ზე მეტია?
3. `allAboveTen` — ყველა ტემპერატურა `10`-ზე მეტია?
4. `anyFreezing` — არის თუ არა რომელიმე ტემპერატურა `0`-ზე ნაკლები?

შემდეგ **`forEach`**-ით დაბეჭდეთ ყოველი ტემპერატურა ცალკე ხაზზე, ფორმატით `12 degrees`.

მოსალოდნელი შედეგი:

```text
anyHot: true
allAboveZero: true
allAboveTen: false
anyFreezing: false
12 degrees
18 degrees
25 degrees
30 degrees
7 degrees
```

შემდეგ უპასუხეთ ამ კითხვას კომენტარში, ფაილის ბოლოში:

**`temperatures.some((t) => t > 28)` და `temperatures.filter((t) => t > 28)` ერთსა და იმავე კითხვას სვამენ. რით განსხვავდება მათი პასუხები?**

> მინიშნება: `forEach` თქვენს callback-ს ყოველ ელემენტზე ერთხელ იძახებს და არაფერს აბრუნებს. ის `for...of` ციკლის ანალოგია მასივის მეთოდებში — გამოიყენეთ მაშინ, როცა ყოველ ელემენტთან მხოლოდ *რაღაცის გაკეთება* გინდათ და არა ახალი მასივის აგება.

---

## `exercise_5.js` — ერთად

ჩააკოპირეთ ეს მასივი თქვენს ფაილში:

```javascript
const movies = [
    { title: "Inception", year: 2010, rating: 8.8, minutes: 148 },
    { title: "The Room",  year: 2003, rating: 3.7, minutes: 99  },
    { title: "Parasite",  year: 2019, rating: 8.5, minutes: 132 },
    { title: "Cats",      year: 2019, rating: 2.8, minutes: 110 },
    { title: "Whiplash",  year: 2014, rating: 8.5, minutes: 106 },
];
```

ფილმი **კარგია** მაშინ, როცა მისი რეიტინგი `8` ან მეტია.

1. შეინახეთ კარგი ფილმები `const`-ში სახელად `good` (ისინი ერთზე მეტჯერ დაგჭირდებათ).
2. `goodTitles` — კარგი ფილმების სათაურები.
3. `goodMinutes` — რამდენი დრო სჭირდება ყველა კარგი ფილმის ნახვას.
4. `best` — მთელ მასივში ყველაზე მაღალრეიტინგული ფილმის **სათაური**.
5. დაწერეთ ისრიანი ფუნქცია `summary(list)`, რომელიც ქვემოთ მოცემულის მსგავს წინადადებას აბრუნებს, და დაბეჭდეთ `summary(good)`.

მოსალოდნელი შედეგი:

```text
goodTitles: [ 'Inception', 'Parasite', 'Whiplash' ]
goodMinutes: 386
best: Inception
summary: 3 good movies · 386 min
```

> მინიშნება მეოთხესთვის: ეს იგივეა, რაც მე-3 სავარჯიშოს `biggest`, ოღონდ აქამდე მიღებული შედეგი რიცხვის ნაცვლად მთლიანი ფილმის ობიექტია. დაიწყეთ `movies[0]`-დან, შეადარეთ `.rating` და დააბრუნეთ გამარჯვებული — შემდეგ კი აიღეთ იმის `.title`, რაც მიიღეთ.
>
> `·` სიმბოლო შუა წერტილია. დააკოპირეთ ის ამ გვერდიდან თქვენს შაბლონურ სტრიქონში.

---

## წესები

შეგიძლიათ გამოიყენოთ:

* ოპერატორები (`+ - * / % ** === !== < >= && || !`), `.length`, მასივის ინდექსები, `Math`-ის მეთოდები
* `const` და `let`
* შაბლონური სტრიქონები (template literals) და სტრიქონის მეთოდები
* მასივები და ობიექტები
* ისრიანი ფუნქციები, პარამეტრები და `return`
* **`map`, `filter`, `find`, `reduce`, `some`, `every` და `forEach`**

**ამ დავალებაში** დაუშვებელია:

* `for`, `while` და `for...of` — ყოველი სავარჯიშო ზემოთ ჩამოთვლილი მეთოდებით იხსნება და სწორედ ეს არის ამ დავალების აზრი
* `.push()` — ამ მეთოდებს ის არ სჭირდებათ. თუ ცარიელ მასივში push-ს აკეთებთ, ესე იგი იგივე ციკლს წერთ, უბრალოდ ზედმეტი ნაბიჯებით
* `var` — გამოიყენეთ `const` ან `let`
* `prompt()` — ის Node.js-ში არ არსებობს, ამიტომ ფაილი ჩავარდება

ყოველი callback დაწერეთ **ისრიანი ფუნქციის** სახით.

> ამ ციკლების ხელით დაწერა უკვე იცით — სწორედ ამიტომ შეგიძლიათ ახლა უფრო მოკლედ დაწეროთ. თითოეული ეს მეთოდი არის `for` ციკლი, რომელიც სხვამ უკვე დაწერა თქვენს მაგივრად; თქვენ მხოლოდ ის ნაწილი უნდა მიაწოდოთ, რომელიც ყოველ ჯერზე განსხვავდება — ანუ callback.

---

როცა თქვენი ხუთი ფაილი მზად იქნება, გადადით **[როგორ ჩააბაროთ](./SUBMITTING_ka.md)** გვერდზე.
